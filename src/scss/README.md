# SCSS 架构重构说明

## 📝 变更概述

将所有 SCSS Mixins 从 `common.scss` 提取到独立的 `mixins.scss` 文件中，优化代码组织结构。

## 📂 新文件结构

```
src/scss/
├── common.scss          # 主文件（变量 + 引入 mixins）
├── mixins.scss          # 所有 Mixins 定义（新增）
├── global.scss
├── helpWidgets.scss
└── index.scss
```

## 🔄 变更内容

### common.scss
- **保留**：所有 SCSS 变量定义（颜色、字体、z-index 等）
- **新增**：`@import './mixins.scss';` 引入 mixins
- **移除**：所有 mixin 定义和 keyframes 动画

### mixins.scss（新增文件）
包含所有 60+ 个 Mixins，按功能分类：

1. **Layout Mixins** - 布局相关
2. **Text Mixins** - 文本处理
3. **Shadow Mixins** - 阴影效果
4. **Animation Mixins** - 动画效果
5. **Responsive Mixins** - 响应式断点
6. **Utility Mixins** - 工具类
7. **Effect Mixins** - 视觉效果
8. **Image Mixins** - 图片处理
9. **Grid Layout Mixins** - Grid 布局
10. **Reset Mixins** - 样式重置

## ✅ 优势

### 1. **更好的代码组织**
- 变量和 Mixins 分离，职责明确
- Mixins 按功能分类，便于查找和维护

### 2. **提升可维护性**
- 修改 Mixins 时不影响变量定义
- 独立文件更易于版本控制和代码审查

### 3. **增强复用性**
- 可以在其他项目中单独复用 mixins.scss
- 便于创建 mixin 库和共享

### 4. **提高可读性**
- common.scss 更简洁（从 841 行减少到 79 行）
- mixins.scss 有详细的分类注释

## 🔧 使用方式

### 保持不变
在组件 SCSS 文件中的使用方式**完全不变**：

```scss
/**
 * @file components/MyComponent/MyComponent.scss
 * @author leon.wang
 */
@import 'scss/common.scss';

.my-component {
  @include flex-center;
  @include shadow(2);
  @include mobile {
    padding: 12px;
  }
}
```

### 原因
因为 `common.scss` 已经引入了 `mixins.scss`，所以导入 `common.scss` 就能使用所有 mixins。

## 📊 文件大小对比

| 文件 | 重构前 | 重构后 |
|------|--------|--------|
| common.scss | 841 行 | 79 行 |
| mixins.scss | - | 750+ 行 |

## ⚠️ 注意事项

1. **向后兼容**：所有现有代码无需修改，完全兼容
2. **导入顺序**：始终在 SCSS 文件顶部导入 `@import 'scss/common.scss';`
3. **变量作用域**：mixins.scss 可以使用 common.scss 中定义的变量

## 🎯 最佳实践

### ✅ 推荐
```scss
// 组件样式文件
@import 'scss/common.scss';  // 包含变量和所有 mixins

.component {
  @include flex-center;
  color: $theme-primary;
}
```

### ❌ 不推荐
```scss
// 不要单独导入 mixins.scss（会缺少变量定义）
@import 'scss/mixins.scss';  // ❌ 缺少变量
```

## 📚 相关文档

- [SCSS Mixins 使用指南](/app/css/scss-mixins) - 查看所有 Mixins 的完整文档和示例
- [项目开发规范](/.github/instructions/00-dev.instructions.md) - 完整的 SCSS 使用规范

## 🚀 下一步

- ✅ 重构完成，所有 Mixins 已提取
- ✅ 文件结构优化，代码组织更清晰
- ✅ 保持向后兼容，无需修改现有代码

如需添加新的 Mixin，请在 `src/scss/mixins.scss` 中对应的分类下添加。
